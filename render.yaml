services:
  - type: web
    name: snake-spectacle
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: snake-spectacle-db
          property: connectionString

databases:
  - name: snake-spectacle-db
    plan: free
