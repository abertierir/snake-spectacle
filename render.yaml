services:
  - type: web
    name: snake-spectacle
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: snake-spectacle-db
          property: connectionString
      - key: PORT
        value: "8000"

databases:
  - name: snake-spectacle-db
    plan: free
